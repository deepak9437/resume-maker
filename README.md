# Resume Maker (EMPTY TEMPLATE)

This is an **empty project zip** containing the folder structure for the Resume Maker web app. Replace placeholder files with the full code snippets provided in the ChatGPT response.

Instructions:
1. Copy the backend Java files into `backend/src/main/java/com/resumemaker/...`
2. Paste the frontend HTML/CSS/JS files into `frontend/` and `frontend/assets/`.
3. Update `application.properties` with your MongoDB Atlas URI.
4. Build the backend with Maven and open the frontend in VS Code Live Server.
